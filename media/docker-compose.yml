version: '3.7'

services:
  plex:
    image: plexinc/pms-docker
    container_name: plex
    privileged: true
    restart: always
    hostname: rhea
    environment:
      TZ: America/New_York
      #PLEX_CLAIM: <claimToken>
    devices:
      - /dev/dri:/dev/dri
    ports:
      - 32410:32400/tcp
    volumes:
      - /home/alex/docker/volumes/plex/config:/config
      - /merge/disk1:/data
      -
        type: tmpfs
        target: /transcode
        tmpfs:
          size: 50000
